const repl = require('repl');
const Client = require("./index");

const replServer = repl.start({
  prompt: 'KV> ',
});

// Make your library available in the REPL context
replServer.context.Client = Client;